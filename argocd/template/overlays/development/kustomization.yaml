apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - path: chart_version.yaml
  - path: chart_values.yaml
  - target:
      group: argoproj.io
      version: v1alpha1
      kind: Application
      name: nginx
    patch: |-
      - op: replace
        path: /spec/destination/namespace
        value: nginx-development
  - target:
      group: argoproj.io
      version: v1alpha1
      kind: Application
      name: nginx
    patch: |-
      - op: replace
        path: /metadata/labels/environment
        value: development
  - target:
      group: argoproj.io
      version: v1alpha1
      kind: Application
      name: nginx
    patch: |-
      - op: replace
        path: /metadata/name
        value: nginx-development