apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: '#{ArgoCD.Project.Name | ToLower | Replace "[^a-zA-Z0-9]" "_"}'
  namespace: argocd
spec:
  source:
    helm:
      values: |
        serverBlock: |-
          server {
            listen 0.0.0.0:8080;
            location / {
            return 200 "hello!";
          }
        }